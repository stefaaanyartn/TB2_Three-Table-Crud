<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stefany.three_table_crud"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="ExtraText">

    // Nama: Stefany Artana
    // NIM : 41522010079
    // Mobile Programming

    <application
        android:allowBackup="true"
        android:name=".Util.MyApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.THREE_TABLE_CRUD"
       >
        <activity
            android:name=".StudentCrud.StudentListActivity"
            android:label="@string/student_list"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SubjectCrud.SubjectListActivity"
            android:label="@string/title_activity_subjects"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TakenCrud.StudentTakenSubjectActivity"
            android:label="@string/student_and_taken_subject"
            android:screenOrientation="portrait" />
        <activity android:name=".TakenCrud.SubjectAssignActivity"
            android:label="@string/subject_assign"
            android:theme="@style/Theme.AppCompat.Light.Dialog"/>
    </application>

</manifest>